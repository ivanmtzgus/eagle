<p id="notice"><%= notice %></p>

<div class="jumbotron">
<h2>EFS Directory is the best ways for companies to connect</h2>
<h3>Explore the bast diractory of the Eagle Ford Shale.<br>
 This is the first site were Services, Safety and Prices get reviewed and rater by Real People</h3>
</div>

<table class="table table-hover talbe-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Service</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @companies.each do |company| %>
      <tr>
        <td><%= company.name %></td>
        <td><%= company.service %></td>
        <td><%= company.address %></td>
        <td><%= company.phone %></td>
        <td><%= link_to company.website, company.website %></td>
        <td><%= link_to 'Show', company, class: "btn btn-link" %></td>
        <td><%= link_to 'Edit', edit_company_path(company), class: "btn btn-link" %></td>
        <td><%= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Company', new_company_path, class: "btn btn-link" %>



<%= image_tag "https://s3.amazonaws.com/eagleford2/eagleford.jpg" %>